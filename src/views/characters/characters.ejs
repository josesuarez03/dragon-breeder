<div class="container">
    <h1>Personajes</h1>
    
    <div class="characters-view">
        <a href="/characters/new">Crear nuevo personaje</a>
        <ul>
            <% characters.forEach(character => { %>
                <li>
                    <%= character.name %> - 
                    <a href="/characters/<%= character.id %>/edit">Editar</a> - 
                    <form action="/characters/<%= character.id %>/delete" method="post" style="display:inline;">
                        <button type="submit">Eliminar</button>
                    </form>
                    <form action="/game/select" method="post" style="display:inline;">
                        <input type="hidden" name="characterId" value="<%= character.id %>">
                        <button type="submit">Seleccionar</button>
                    </form>
                </li>
            <% }); %>
        </ul>
    </div>
    
</div>